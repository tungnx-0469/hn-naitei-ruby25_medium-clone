<%= form_for([article, current_user.favorites.build]) do |f| %>
  <div><%= hidden_field_tag :article_id, article.id %></div>
  <%= button_tag(type: 'submit', class: "action-btn") do %>
    <%= bootstrap_icon "heart", height: Settings.default.view.edit_profile.icon_size %>
    <%= article.like_count %>
  <% end %>
<% end %>
